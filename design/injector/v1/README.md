# MyiUI Injector Design v1

进程外注入器界面定稿，与主菜单 `design/v1/` 共享品牌视觉。

- `tokens.json` — 颜色、间距、窗口尺寸
- `layout.json` — 区域与控件结构
- `preview/index.html` — 浏览器预览（Open Design 对齐用）

预览：在浏览器中打开 `preview/index.html`。
